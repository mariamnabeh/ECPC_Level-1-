#include <iostream>
#include <algorithm>
#include <string>
#include <vector>
#include <queue>
#include <deque>
#include <stack>
#include <set>
using namespace std;

#define el "\n"
typedef long long ll;
typedef long double ld;
void solve()
{
    

    int t;
    cin >> t;

    while (t--) {

        ll n, x;
        cin >> n >> x;
        ll minm=1e9;
        vector<ll> a(n);
        for (int i=0; i<n; i++){
cin>>a[i];
minm=min (minm,a[i]);




        }



         }
}


int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);

    solve();

    return 0;
}
